# Conference logos

Drop conference logo files here (SVG or PNG, ideally single-color/transparent),
then reference them from `src/data/conferences.yaml`:

```yaml
- name: ApacheCon
  logo: /logos/apachecon.svg
```

Rows without a `logo:` render as styled text.
